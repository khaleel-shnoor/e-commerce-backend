# Development / CI — install after production deps
# pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.3.5
pytest-asyncio==0.25.3
black==24.10.0
isort==5.13.2
pylint==3.3.6
